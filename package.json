{
  "dependencies": {
    "firebase": "^10.12.2"
  },
  
    "name": "flutter-firebase-app",
    "version": "1.0.0",
    "scripts": {
      "build": "flutter build web",
      "deploy": "firebase deploy"
    },
    "devDependencies": {
      "firebase-tools": "^9.0.0"
    }
  
  
}
